# Shared Web

React client infrastructure shared by the public web and admin applications. It owns
the Query provider, API client boundary, shared form schema, and basic async-state
primitive. It must not contain application-specific routes or business logic.
