import { Module } from '@nestjs/common';

// The directories listed below reserve the bounded contexts; each later gets api/application/domain/infrastructure.
@Module({})
export class PlaceholderModulesModule {}
